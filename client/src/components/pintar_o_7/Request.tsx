import { Box, Paper } from '@mui/material';

export default function Request() {
    return (
        <Paper
            sx={{
                width: '100%',
            }}>
            <Box component={'div'} sx={{ padding: '1rem' }}></Box>
        </Paper>
    );
}
